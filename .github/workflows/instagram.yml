name: Instagram Auto Post

on:
  workflow_dispatch

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: node run_fetch.mjs
      - run: node run_instagram.mjs
